from django.shortcuts import render

# Create your views here.
def index(request):
    index_placeholder = "placeholder to later display a list of all surveys"
    print(index_placeholder)
    return index_placeholder